version: '3.5'

services:
  app:
    build:
      context: ./
      dockerfile: dev.Dockerfile
    image: kliksutin_dev
    volumes:
      - ./:/usr/src/app
    ports:
      - 3000:3000
    container_name: kliksutin_dev
